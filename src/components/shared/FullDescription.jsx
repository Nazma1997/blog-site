import React from 'react'

const FullDescription = () => {
  return (
    <div className='text-white  min-w-fit	' >
      {/* <div className='flex justify-center'> */}
     {/* <p className=' px-5 py-4 text-xl font-semibold text-white  bg-gray-800 mb-5'></p> */}
     {/* </div> */}
     <p> Tailwind lets you conditionally apply utility classes in different states using variant modifiers. For example, use hover:min-w-0 to only apply the min-w-0 utility on hover.Tailwind lets you conditionally apply utility classes in different states using variant modifiers. For example, use hover:min-w-0 to only apply the min-w-0 utility on hover.Tailwind lets you conditionally apply utility classes in different states using variant modifiers. For example, use hover:min-w-0 to only apply the min-w-0 utility on hover.Tailwind lets you conditionally apply utility classes in different states using variant modifiers. For example, use hover:min-w-0 to only apply the min-w-0 utility on hover. </p>
    </div>
  )
}

export default FullDescription
